#include "main.h"
#include <stdio.h>

/**
 * _isupper - checks if input is an uppercase letter in ASCII
 * @c: parameter
 * Return: 1 if c is uppercase or otherwise
 */
int _isupper(int c)
if (c >= 'A' && c <= 'Z')
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
